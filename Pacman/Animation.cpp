#include "Animation.h"
#include <SDL_image.h>

Animation::Animation( )
{
	// We hardcode an image for now
	//SDL_Surface* surface = IMG_Load(imageName);
	//SDL_Texture* newTexture = SDL_CreateTextureFromSurface(renderer, surface);
	//spriteList.push_back(newTexture);
}

Animation::~Animation()
{
}
